name: github

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - env:
          SUPER_SECRET: CIAO
      run: echo "$SUPER_SECRET"
    


    
